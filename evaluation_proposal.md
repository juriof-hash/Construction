지적하신 7가지 사항들이 모두 기하학적 엄밀성(수학적 예외 상황, 부동소수점 오차, 교육적 목적)을 꿰뚫고 있습니다. 특히 헤론의 공식 대신 외적(Cross Product)을 사용하자는 점, 그리고 내심 작도의 특이성이 없으므로 미션을 분리할 필요가 없다는 점은 매우 타당합니다.

말씀하신 피드백을 반영하여 수정한 **스테이지 5 (수선 및 내심/내접원 작도) 정답 확인 로직 설계**입니다.

### 🔴 버그 유발 요소 수정 (피드백 1~4)
1. **[5-1 직교 검사] 벡터 정규화(Normalize) 추가**: 
   - `v1`과 `v2`를 단위 벡터로 변환한 뒤 내적(`dot(n1, n2)`)을 구하고, 그 절대값이 허용 오차(`EPSILON_ANGLE = 0.02`) 미만인지 확인합니다. 긴 선분이나 짧은 선분에 관계없이 방향만 엄밀하게 판단합니다.
2. **[5-1 점 포함 및 수선의 발 교차 검사]**: 
   - 선분이 점 $P$를 지나는지 확인하는 것에 더해, $P$에서 원본 직선 $l$에 내린 수선의 발이 **사용자가 그린 선분 위에 실제로 존재하는지** 검사합니다. 방향만 맞고 선에 닿지 않는 빗나간 선분을 걸러냅니다.
3. **[5-3 내접원 데이터 검증] 컴퍼스 오차 한도 여유 및 스냅 요구**:
   - 내부적으로 원은 `{center, r}` 형태로 저장됩니다. `r`은 중심과 사용자가 찍은 `radiusPt` 간의 거리로 결정됩니다.
   - 스냅하지 않고 육안으로 그렸을 경우를 대비하여 반지름 비교 시 `EPSILON_DIST`(5 단위) 수준의 마진을 허용합니다. (단, 정확한 내심 좌표를 잡는 것은 앞선 각의 이등분선 과정 검증으로 강제됩니다.)
4. **[면적 계산] 외적(Cross Product) 사용**:
   - 얇고 긴 둔각삼각형에서 소거 오차를 유발하는 헤론의 공식 대신 `Area = |cross(B-A, C-A)| / 2` 공식을 내접원 반지름( $r = 2 \times Area / (a+b+c)$ ) 계산의 표준으로 채택합니다.

### 🟡 설계 결정 및 구체화 (피드백 5~7)
5. **[5-2 과정 검증] "각의 이등분선" 작도 여부 검사**:
   - 단순히 내심 근처에 점만 찍어 통과하는 꼼수를 차단합니다. (기존 외심 미션의 수직이등분선 검사와 동일한 철학)
   - **검사 로직**: 사용자가 추가한 직선(line/segment) 중, "꼭짓점에서 출발하고" "해당 꼭짓점의 각도를 이등분하는 방향(오차 `EPSILON_ANGLE` 이내)"인 선분이 최소 2개 이상 존재하는지 먼저 검사합니다.
6. **[5-3~5-5 통합] 교육적 근거에 따른 미션 축소**:
   - 외심과 달리 내심은 삼각형 형태(예각/직각/둔각)에 상관없이 항상 내부에 존재하므로 반복의 의미가 적습니다. 5-3, 5-4, 5-5를 **"5-3: 삼각형의 내접원 작도" 하나로 통합**합니다. (스테이지 5는 총 3개 미션으로 구성됩니다.)
7. **[상대 오차 (EPSILON) 공식 명시화]**:
   - 오차 기준을 모든 스테이지 5 미션에 동일하게 적용하기 위해 고정 상수로 관리합니다.
   - `EPSILON_DIST = 5.0` (좌표간 거리 허용 오차)
   - `EPSILON_ANGLE = 0.02` (각도 허용 오차: 단위 벡터 내적 기준, 약 1.14도)

---
이 수정된 설계대로 코드를 작성하면 수선 작도 시의 기하학적 예외와, 꼼수로 점만 찍어 통과하는 문제를 완벽히 차단할 수 있습니다. 

이 설계안으로 미션 5-1(수선), 5-2(내심), 5-3(내접원) 작도 로직 구현을 시작할까요?
