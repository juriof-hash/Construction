1. **수동 변경 시 중복 검사 및 커밋 시점 (Blur/Enter)**:
   - `StylePopup` 컴포넌트에 점 라벨을 수정할 수 있는 `<input>`을 추가하고, 두께 슬라이더와 동일하게 로컬 state로 관리합니다.
   - `onBlur` 또는 `Enter` 키 입력 시점에만 `UPDATE_GEOMETRY_LABEL` (또는 기존 스타일 업데이트 액션 확장) 액션을 디스패치합니다.
   - 리듀서(Reducer) 내부에서 중복 여부(이미 존재하는 점의 이름인지) 및 빈 값 등을 검사하여 안전한 값만 적용합니다.

2. **자동 이름 부여 규칙 (I, O 스킵)**:
   - `GeometryContext.tsx`의 `ADD_GEOMETRY` 리듀서에서, 추가되는 도형이 `point`이고 라벨이 없으면 현재 존재하는 점들의 라벨을 분석하여 가장 빠른 빈 알파벳을 찾습니다.
   - 교육적 관례에 따라 숫자 1, 0과 혼동되기 쉬운 `I`와 `O`는 후보에서 제외하는 로직을 기본으로 적용하겠습니다.

3. **기존 도형(선분, 미션 도형)과의 연동 여부**:
   - `src/missions/` 및 `stage` 생성기 파일들을 grep해본 결과, 채점 로직 자체는 `label` 문자열에 전혀 의존하지 않고 좌표와 위상 기하만으로 검증됩니다.
   - 단, 미션 지문("점 A와 점 B...")과의 UX 불일치를 막고 파생 도형("선분 AB")의 복잡도를 피하기 위해, **초기 미션 도형(`source === 'initial'`)은 이름 수정(Input)을 비활성화(disabled)** 하겠습니다. 사용자 생성(`user`) 점에 대해서만 자유로운 이름 변경을 허용합니다.

4. **두 번째 호 스타일 변경 버그 원인 및 수정**:
   - 호(Arc)의 Hit-Test(선택) 수학 로직 자체는 교차각, 음수 각도 스윕 등을 완벽히 커버하도록 정상 작동 중입니다.
   - "스타일 변경이 작동 안 한다"고 느끼신 주된 이유는 지난 패치에서 선(Line)에는 점선 처리를 위한 `strokeLinecap="round"`를 적용했지만, **원(Circle)과 호(Arc)에는 이를 누락**했기 때문입니다. 이로 인해 '점선(dotted)' 스타일을 눌러도 두께가 얇거나 각진 형태가 되어 스타일이 안 변한 것처럼 보였을 확률이 가장 높습니다. 
   - `ObjectLayer.tsx`에서 호와 원에도 라인캡 렌더링 속성을 추가하여 시각적 버그를 수정하겠습니다.
